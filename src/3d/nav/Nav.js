export default class Nav {
  constructor(grid) {
    this.grid = grid;
  }

  pathfind(start, end) {
    return [];
  }

  costMap(start, max) {
    return new Map();
  }

  isBlocked() {
    return false;
  }
}
